# Secrets
.env
credentials/
*.pem
*.key

# ── MC Simulator outputs (regenerated by run_phase3_baselines.py / run_mc_simu.py / run_daily.py) ──
# Inputs (results.csv, matches_*, elo_history*, cache/, wc2026_*, r32_seeding_table.json,
# clubelo_cache.json, eloratings_top20_*, tournament_meta.json, ucl_*) ARE tracked.
data/mc_simu/phase3_baselines/
data/mc_simu/mc_wc2026_*.csv
data/mc_simu/loto_cv_*.csv
data/mc_simu/replay_*
data/mc_simu/_*.json
data/mc_simu/daily_log.csv
data/mc_simu/market_history.csv
data/mc_simu/group_winner_log.csv

# Internal audit + preliminary report docs — kept local, not pushed.
# (scope*.md, before_world_cup_audit.md, mv_historical_validation.md in audits/ ARE tracked.)
mc_simu/audits/audit_overrides.md
mc_simu/audits/audit_report.md
mc_simu/audits/phase0_audit_report.md
mc_simu/audits/preliminary_report.md
mc_simu/presentation/
mc_simu/archive/

# Generated figures/diagrams — regenerated locally, not pushed (ship code + inputs).
mc_simu/figures/

# Transfermarkt raw dataset cache (dcaribou R2) — regenerate locally, do not commit
data/mc_simu/cache/tmdata/

# Scraper debug HTML
data/*.html
data/**/*.html
# Whitelist mc_simu cache — Wikipedia snapshots committed for offline reproducibility.
# Must come AFTER `data/**/*.html` to override it.
!data/mc_simu/cache/**/*.html

# ── Claude session files ───────────────────────────────────────────────────────
.claude/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── OS / IDE ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# Run logs + internal phase reports — local only
logs/
mc_simu/phase0_audit_report.md
data/mc_simu/reach_log.csv

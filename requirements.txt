# Core simulation — upper bounds block untested majors mid-tournament
numpy>=1.26.0,<3
scipy>=1.13.0,<2
pandas>=2.2.0,<4

# Market fetch + historical scraping (Wayback / Wikipedia cache)
requests>=2.31.0,<3
beautifulsoup4>=4.12.0,<5

# Figures
matplotlib>=3.8.0,<4

# Progress bars
tqdm>=4.66.0,<5

# Dashboard (Streamlit Cloud installs this file; CI installs only the core four)
streamlit>=1.35.0,<2
plotly>=5.22.0,<7

# Tests
pytest>=8.0.0
